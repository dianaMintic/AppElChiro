<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiendaelchiro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaElChiro">
        <activity android:name=".datosrden"></activity>
        <activity android:name=".resumenPedido" />
        <activity android:name=".PantallaBusqueda" />
        <activity android:name=".Registro" />
        <activity android:name=".PantalladeInicio"></activity>
        <activity
            android:name=".PantallaInicial"
            android:label="@string/title_activity_pantalla_inicial"
            android:theme="@style/Theme.TiendaElChiro.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>